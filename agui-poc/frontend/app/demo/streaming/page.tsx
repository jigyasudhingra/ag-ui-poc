export default function Page() {
  return (
    <p className="max-w-prose text-sm leading-relaxed text-zinc-400">
      Open the sidebar chat (shortcut /). Streaming tokens appear live; questions about current
      events can trigger Tavily via the backend agent when configured.
    </p>
  );
}
